
if (AMD_GPU)
    set(QuEST_SRC_ARCHITECTURE_DEPENDENT
        ${CMAKE_CURRENT_SOURCE_DIR}/QuEST_gpu.hip
        PARENT_SCOPE
    )
else()
    set(QuEST_SRC_ARCHITECTURE_DEPENDENT
        ${CMAKE_CURRENT_SOURCE_DIR}/QuEST_gpu.cu
        PARENT_SCOPE
    )
endif()

